# 2026-07-12

## One-liner fixes
- Status: Fixed - Removed Sales-side draft URL rewrite to `/run-v2-lite` and restored real `/run-v2` draft handoff behavior.
- Status: Fixed - Kept tunnel reroll robustness while preserving the same linked draft run path/query.
- Status: Verified - Sales build passes after rollback/normalization updates.

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Draft dashboard URL normalization now keeps `/run-v2` and strips stale retry query only.
- `server.js` - Maker dashboard path normalization now keeps `/run-v2` draft links while cleaning stale retry query.

## Action items / open
- [ ] Push this Sales rollback patch to `main` after user confirms this is the intended flow.

## Client update (WhatsApp format)
- ✅ Removed the temporary Stable Mode link rewrite from Sales and restored normal Run V2 draft opens.
- ✅ Tunnel reroll still keeps the same connected run; no forced new run path added by Sales normalization.
