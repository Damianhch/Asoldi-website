## One-liner fixes
- Status: Fixed - Imported full WAV batch from Downloads into local `myphoner-audio` store for one-time Sales backfill.
- Status: Fixed - Manually mapped all remaining unmatched recording phone numbers to explicit Sales clients (including creating missing leads where no client existed).
- Status: Fixed - Added local-asset fallback in recording proxy so mapped `myphoner-audio` files stream as playable audio instead of HTML fallback.
- Status: Verified - Playback proxy returns audio bytes (`audio/wav`) for newly mapped clients.

## Files changed
- `server.js` - Added local recording asset resolution + MIME handling in `/api/admin/sales/:id/recording` proxy path.
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Removed temporary manual ops buttons from Sales cards per user request.
- `public/myphoner-audio/*` - Added bulk WAV files from local Downloads for one-time data backfill.

## Action items / open
- [ ] Publish latest backend/UI cleanup commit so production uses the same local-audio proxy fallback.
- [ ] Confirm if the remaining no-recording leads should be left empty or receive additional manual uploads.
- [ ] Optional: merge or archive the historical `Sync Smoke` test lead if no longer needed.

## Client update (WhatsApp format)
- ✅ La inn hele lydpakken fra Downloads og koblet resten manuelt til riktige kunder.
- ✅ Alle tidligere umatchede opptak er nå mappet til konkrete leads (manglende leads ble opprettet der det trengtes).
- ✅ Fikset avspilling slik at lokale lydfiler spilles direkte i Sales (ikke HTML-feil).
- 🎧 Testet avspilling på nye mapper og bekreftet at lyd faktisk leveres.

## Update - 2026-06-28 (production audio visibility)
### One-liner fixes
- Status: Fixed - Added startup recording backfill so deployed servers auto-attach local `myphoner-audio` files to Sales clients on boot.
- Status: Fixed - Enabled fill-missing-only mode so startup sync does not overwrite existing live recording links.
- Status: Verified - Startup logs now print deterministic backfill summary (`files`, `updated`, `skippedExisting`, `unmatched`).

### Files changed
- `server.js` - Extended `syncLocalMyphonerRecordings` with `fillMissingOnly` support and called it from `ensureData()` startup path.

### Action items / open
- [ ] Deploy this startup hotfix commit and verify production logs include `[sales] startup recording backfill`.
- [ ] Refresh Sales UI after deploy to confirm audio buttons appear without manual sync actions.

### Client update (WhatsApp format)
- ✅ Lagt inn automatisk backfill ved server-start, så lyd kobles inn uten manuell knapp.
- ✅ Satt den til \"fill missing only\" slik at eksisterende opptak ikke overskrives.
- ✅ Verifisert lokalt at startup logger synkstatus tydelig, klar for produksjonssjekk.

## Update - 2026-06-28 (sales domain + auto link enrichment)
### One-liner fixes
- Status: Fixed - Blocked and auto-cleaned placeholder `*.myphoner.com` website domains from Sales clients (including `asoldi.myphoner.com`).
- Status: Fixed - Added automatic link enrichment on Myphoner winner intake to fill missing Google Business, Proff, Instagram, and Facebook URLs.
- Status: Improved - Added BRREG/org-number assist + scored search fallback to improve social/profile match precision when lead metadata is sparse.

### Files changed
- `server.js` - Added domain sanitizer + startup cleanup and new post-webhook client-link enrichment pipeline.
- `data/sales.js` - Normalized `websiteDomain` to reject `*.myphoner.com` placeholders at persistence layer.
- `.env.example` - Documented new Myphoner auto-link enrichment environment toggles/timeouts.

### Action items / open
- [ ] Validate one full production winner flow end-to-end and confirm links auto-populate on the client card without manual edits.
- [ ] Spot-check 10 newly enriched clients to tune search thresholds if any Facebook/Instagram mismatch appears.

### Client update (WhatsApp format)
- ✅ Fjernet feil `asoldi.myphoner.com`/`*.myphoner.com` som nettsidedomene i Sales.
- ✅ Ny automatisk flyt ved Myphoner-vinner: fyller inn Google Business, Proff, Instagram og Facebook når data mangler.
- ✅ Bruker metadata først (inkl. orgnr/Maps), med trygg søk-fallback for høyere treff på riktig konto.
