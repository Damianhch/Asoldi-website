# 2026-08-14

## One-liner fixes
- Parallel sales create-run was only on a feature branch, not asoldi.com. Status: Deployed to main
- SSU winners now copy into the MyPhoner "SSU wins" list via the existing winner webhook, plus a backfill of historical SSU wins (not only current `won` status). Status: Fixed

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` — unique create-run popups + parallel in-flight creates, merged to `main`
- `lib/myphoner-ssu-wins.js` — copy SSU winner leads into "SSU wins", including historical winner events and comments
- `lib/myphoner-api.js` — create/find/list-all lead helpers + 429 retry
- `data/myphoner-integration.js` — remember already-copied SSU winner lead IDs
- `server.js` — copy on winner webhook, startup/reconcile catch-up, admin backfill endpoint
- `scripts/backfill-ssu-wins.mjs` — one-shot backfill of current SSU winners
- `.env.example` — SSU wins list env knobs

## Action items / open
- [ ] Confirm Hostinger finished deploying `b5febe7` to https://asoldi.com/sales
- [ ] Website Maker parallel scrape/pipeline still lives on the Docker host, not Hostinger
- [ ] Confirm next live SSU winner appears in MyPhoner list "SSU wins"
- [x] Confirm historical SSU winners (20 still in MyPhoner) were copied into "SSU wins" with comments

## Client update (WhatsApp format)
asoldi.com/sales did not have the parallel-run UI yet — it only lived on a branch.
Pushed it to production main so Hostinger can deploy. Creating two client runs from Sales should no longer steal the same popup.
The actual website builds still run on the office Docker Maker.

SSU winners will now be copied automatically into the MyPhoner list "SSU wins" when marked as Winner on the SSU list, including the lead comment. Existing SSU wins (also those later moved to callback) have been backfilled into that list.
