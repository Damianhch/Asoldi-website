# 2026-07-10

## One-liner fixes
- Status: Fixed - Deployed Step 0 meeting-status alignment so Sales card color reflects real agreed meeting data.
- Status: Fixed - Deployed Step 0 click behavior to open Edit Client for real meeting-time updates instead of a detached progression toggle.
- Status: Deployed - Pushed calendar/status patch to `main` (`d65c1a4`) for production rollout.

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Step 0 card status source + click behavior.
- `data/sales.js` - Progression normalization for `step0AgreeMeetingTime` now mirrors real `agreedTime`.
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - Marked deploy checklist item complete.

## Action items / open
- [ ] Validate one live owner-connected client edit on production to confirm calendar event time updates immediately after save.

## Client update (WhatsApp format)
- ✅ Step 0 meeting indicator now follows real agreed meeting data (green only when a time is truly set).
- ✅ Step 0 action now opens edit flow so reps update real meeting time directly.
- ✅ Patch is deployed to main and ready for live verification.

---

## One-liner fixes
- Status: Fixed - Corrected Sales "Open in maker" link resolution so it keeps the run's stored handoff path (including intake draft paths) instead of forcing `/run/<id>`.
- Status: Fixed - Added base-matching guard: only remap stored run links to the currently entered base when origins are compatible (same origin, both local, or both tunnel hosts).
- Status: Fixed - Added backend `websiteMakerBaseUrl` in create-run response and UI consumption so Sales follows the actual maker base used by server.

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Maker link selection/remap safeguards + create-run base sync from response.
- `server.js` - Included `websiteMakerBaseUrl` in `/api/admin/sales/:id/create-maker-run` response.
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - Added checklist for Sales→Maker loading fix.
- `docs/daily-logs/2026-07-10.md` - Appended loading-fix implementation notes.

## Action items / open
- [ ] Validate live Sales flow on active tunnel (`Create website run` → `Open in maker`) and confirm workspace no longer stalls on "Loading workspace state...".

## Client update (WhatsApp format)
- ✅ Fixed: Sales no longer forces the wrong maker route when opening runs; it now respects the saved run handoff path.
- ✅ Fixed: base URL switching is now guarded so links don't silently jump to incompatible hosts.
- ✅ Fixed: create-run now returns and syncs the actual maker base used, keeping Sales UI aligned with backend behavior.

---

## One-liner fixes
- Status: Deployed - Pushed Sales→Maker run loading/routing fix to `Asoldi-website` `main` (`675b4c3`).

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - deployed link remap/base-sync changes.
- `server.js` - deployed create-run response base-url echo.
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - marked deployment checklist item complete.

## Action items / open
- [ ] User-side live validation pending on active tunnel (Create website run → Open in maker).

## Client update (WhatsApp format)
- ✅ Deployed: Sales→Maker loading fix is now live on main.
- ✅ Included: safer run-link routing + backend/UI base alignment to stop stuck workspace opens.

---

## One-liner fixes
- Status: Fixed - Hardened Website Maker local tunnel startup with pre-warm + health checks for `run-v2`, layout chunk, and templates API before returning a tunnel URL.
- Status: Fixed - Changed tunnel provider auto-fallback order to prefer `localhost.run` before `loca.lt` when `cloudflared` is unavailable.
- Status: Fixed - Extended Sales tunnel popup wait timeout so first-start warmup checks can complete instead of timing out early.

## Files changed
- `C:/Asoldi/website maker application/lib/local-tunnel.js` - Added tunnel warm/probe checks and provider fallback order hardening.
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Increased Sales popup timeout for tunnel startup readiness.
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - Added tunnel chunk-timeout hardening checklist.
- `docs/daily-logs/2026-07-10.md` - Appended this tunnel hardening entry.

## Action items / open
- [ ] Re-test from Sales UI with a fresh tunnel URL and confirm `Create website run` + `Open in maker` loads templates/client bundles/run state without chunk timeout.

## Client update (WhatsApp format)
- ✅ Fixed: tunnel setup now verifies critical pages/chunks/API before returning URL to Sales.
- ✅ Fixed: fallback now avoids jumping to less-stable tunnel provider too early.
- ✅ Fixed: Sales waits longer for first tunnel startup so the health checks can finish.

---

## One-liner fixes
- Status: Deployed - Pushed Sales tunnel popup timeout increase to `Asoldi-website` `main` (`1040b00`).

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Increased tunnel popup timeout to 120s with clearer first-run guidance.

## Action items / open
- [ ] Re-test Sales flow with "New tunnel URL" and confirm templates/client bundles/run pages load reliably through the fresh tunnel.

## Client update (WhatsApp format)
- ✅ Deployed: Sales now waits longer during tunnel startup to avoid false timeout while warm checks run.

---

## One-liner fixes
- Status: Fixed - Added Website Maker `npx cloudflared` fallback path so tunnel startup no longer depends only on a globally installed cloudflared binary.
- Status: Fixed - Added tunnel content checks to reject localtunnel warning/interstitial pages as unhealthy startup results.
- Status: Fixed - Added healthy-tunnel reuse so repeated tunnel-start calls keep the same working URL instead of rotating to fresh random domains.
- Status: Fixed - Added dynamic run-page chunk warmup check to reduce first-open `ChunkLoadError` when opening `/run/<id>` from Sales.

## Files changed
- `C:/Asoldi/website maker application/lib/local-tunnel.js` - Tunnel provider fallback, health/content checks, stable reuse logic, run-chunk warmup.
- `C:/Asoldi/website maker application/app/api/system/local-tunnel/start/route.js` - Tunnel start options/metadata (`forceRestart`, `reused`).
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - Added checklist completion for tunnel hardening work.
- `docs/daily-logs/2026-07-10.md` - Appended this cross-repo tunnel stabilization log.

## Action items / open
- [ ] User-side validation pending: confirm Sales -> Open in maker works end-to-end after generating/using a healthy tunnel URL.

## Client update (WhatsApp format)
- ✅ Fixed: tunnel bootstrap now has stronger health/content validation before Sales gets a URL.
- ✅ Fixed: existing healthy tunnel URLs are reused so links stop rotating unnecessarily.
- ✅ Fixed: startup now prewarms run-page chunk to reduce first-open loading failures.

---

## One-liner fixes
- Status: Fixed - Rewrote Sales Maker dashboard link normalization to route draft handoff URLs to `/run-v2-lite` while preserving the same `draftRunId`.
- Status: Fixed - Updated tunnel popup launch parameters to force a fresh tunnel restart consistently from Sales UI.
- Status: Fixed - Added backend-side Maker handoff path normalization so stale stored `/run-v2?draftRunId=...` links are upgraded to stable intake links.

## Files changed
- `app/pages/Admin/sections/SalesClientsSection.tsx` - Stable draft dashboard URL normalization + explicit `forceRestart=1` on tunnel popup.
- `server.js` - Added Maker dashboard-path normalization helper and applied it inside `buildMakerRunLinks`.
- `C:/Users/darac/.cursor/projects/c-Asoldi-website-maker-application/task.md` - Added and completed Sales tunnel + draft continuity checklist.

## Action items / open
- [ ] Validate directly in Sales production UI that **Open in maker** for an existing client keeps the same run after generating a new tunnel URL.

## Client update (WhatsApp format)
- ✅ Fixed: existing client run now stays linked when tunnel URL is regenerated; no forced new run path.
- ✅ Fixed: Sales tunnel button now requests a true fresh tunnel restart consistently.
- ✅ Fixed: stale draft handoff links are auto-upgraded to stable intake links in Sales.
