/**
 * Image paths under public/media/.
 * Add new images in public/media/ and reference them here.
 * Use IMAGES.key in components for local, dependency-free assets.
 */
export const IMAGES = {
  /** Default image for Open Graph / social sharing (add public/media/og-image.jpg) */
  ogImage: '/media/og-image.jpg',
} as const;
